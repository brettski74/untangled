# Drive CI through the root Makefile (install / lint / test).
# Not an autotools project — no ./configure, make check, or make distcheck.
name: Makefile CI

on:
  # Run on every branch push (feature/patch branches included), not only main.
  push:
  pull_request:
    branches: ["main"]

concurrency:
  group: makefile-ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: backend/requirements.lock

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: make install

      - name: Lint
        run: make lint

      - name: Test
        # backend-test starts Postgres via `make db-up` (Compose on the runner).
        run: make test
