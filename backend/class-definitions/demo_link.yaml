# Second demo / fixture class: FK coverage for schema migrate (not a real ticket type).
name: demo_link
display_name: Demo Link
description: >
  Minimal fixture class with a foreign key to demo_item. Used to prove YAML → IR
  → diff → apply for foreign keys. Not a product ticket type.
attributes:
  demo_item_id:
    type: uuid
    required: true
    references: demo_item
  label:
    type: compact_text
    required: true
