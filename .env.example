# Example runtime env for Rocky / `./deploy.sh` (do not commit real secrets).
# Actions writes an equivalent file to the host from Environment secrets.
# Local: copy to `.env` when exercising `./deploy.sh` (gitignored).
# `make up` does not require this file (Compose defaults cover local-dev).
# Local JWT PEMs: `make local-jwt-keys` writes gitignored `deploy/jwt/*.pem`.

POSTGRES_USER=untangled
POSTGRES_PASSWORD=replace-me
POSTGRES_DB=untangled

DATABASE_URL=postgresql://untangled:replace-me@postgres:5432/untangled
# Compose DNS hostname `redis`; local-dev default has no password (document residual risk).
UNTANGLED_REDIS_URL=redis://redis:6379/0

# Access JWTs are ES256. Auth holds the private key; API and web hold the public key only.
# Rocky: CI writes `deploy/jwt/dev-es256-*.pem` from Environment secrets (not this file).
# UNTANGLED_JWT_PRIVATE_KEY_PATH=/jwt/private.pem
# UNTANGLED_JWT_PUBLIC_KEY_PATH=/jwt/public.pem
UNTANGLED_ACCESS_TOKEN_TTL_SECONDS=900
UNTANGLED_REFRESH_TOKEN_TTL_SECONDS=604800
# NDJSON audit log directory inside api/auth containers.
# Local `make up` bind-mounts ./.run/audit here. Rocky `./deploy.sh` uses named
# volume untangled_audit (do not set UNTANGLED_AUDIT_MOUNT on the deploy host).
UNTANGLED_AUDIT_LOG_DIR=/var/log/untangled/audit
# UNTANGLED_AUDIT_ROLLOVER_BYTES=1048576
# UNTANGLED_AUDIT_ROLLOVER_SECONDS=86400

# true behind HTTPS reverse proxy (typical for untangled9.dataphobe.com).
UNTANGLED_COOKIE_SECURE=true
UNTANGLED_API_BASE_URL=http://api:8000
# Exact browser origin (scheme + host + port) for auth CSRF/Origin checks.
UNTANGLED_PUBLIC_ORIGIN=https://untangled9.dataphobe.com

# Local Compose HTTPS origin (Make `local-edge` profile). Rocky uses a customer edge instead.
# UNTANGLED_PUBLIC_ORIGIN=https://localhost:8443
# UNTANGLED_PROXY_HOST_PORT=8443

# Rocky / reverse-proxy hosts typically publish web on 3000.
UNTANGLED_WEB_HOST_PORT=3000
UNTANGLED_AUTH_HOST_PORT=3001
# UNTANGLED_API_HOST_PORT=8000
# UNTANGLED_POSTGRES_HOST_PORT=5432
# UNTANGLED_REDIS_HOST_PORT=6379

# Seed passwords for Rocky: do not put published defaults here.
# `./deploy.sh` regenerates host-local seed-credentials.env every deploy (gitignored).
# SEED_ADMIN_PASSWORD=
# SEED_READONLY_PASSWORD=
# SEED_READWRITE_PASSWORD=
# SEED_CHANGE_PASSWORD=
# SEED_INCIDENT_PASSWORD=
